SP xFP V11 Production Dashboard — https://Kejjeh.github.io/xfp-model/

This is the GitHub Pages source folder. `index.html` is the self-contained
dashboard with all projection data embedded inline as a JavaScript const.

To refresh: copy the latest `data/outputs/xfp_v11_dashboard.html` to `docs/index.html`,
commit, push. Pages redeploys within ~60s.
